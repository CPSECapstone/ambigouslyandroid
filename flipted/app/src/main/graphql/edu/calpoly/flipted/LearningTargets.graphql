query GetAllTargetProgress($courseId: String!,$username: String){
    getAllTargetProgress(courseId: $courseId, username: $username)
    {
        student,
        target {
            targetName
            targetId
        }
        objectives {
            objectiveId
            objectiveName
            tasks {
                taskId
                taskName
                mastery
            }
        }
    }
}

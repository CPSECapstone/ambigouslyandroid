query GetAllTargetProgress($courseId: String!){
  getAllTargetProgress(courseId: $courseId) {
    target {
      targetName
    },
    objectives {
      objectiveId,
      objectiveName
      tasks {
        taskId,
        taskName,
        mastery
      }
    }
  }
}

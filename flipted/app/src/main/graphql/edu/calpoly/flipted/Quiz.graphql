query GetQuiz($quizId: String!) {
  quiz(quizId: $quizId) {
    id
    name
    course
    instructions
    due
    questions {
      id
      quizId
      description
      options {
        id
        description
      }
      answers
      points
    }
  }
}

<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="220dp"
    android:layout_height="220dp"
    xmlns:tools="http://schemas.android.com/tools">

    <ProgressBar
        android:id="@+id/mission_progress"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:indeterminate="false"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:progressDrawable="@drawable/mission_progress_circle"
        android:background="@drawable/circle_shape"
        style="?android:attr/progressBarStyleHorizontal"
        android:max="100"
        android:progress="60"
        />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/mission_item_text"
        android:clickable="true"
        android:onClick="onClick"
        app:layout_constraintStart_toStartOf="@id/mission_progress"
        app:layout_constraintEnd_toEndOf="@id/mission_progress"
        app:layout_constraintTop_toTopOf="@id/mission_progress"
        app:layout_constraintBottom_toBottomOf="@id/mission_progress"
        tools:text="My Sample Mission"
        style="@style/Heading2Style"
        android:paddingHorizontal="30dp"
        android:gravity="center_horizontal"
        android:focusable="true" />

</androidx.constraintlayout.widget.ConstraintLayout>